/**
 * The tokenizer, for the pattern languages that lex notation embedded in
 * their own syntax. `@blockchaincommons/dcbor-parse/lexer`.
 *
 * @module
 */
export { Lexer, type Token, token } from "./token";
